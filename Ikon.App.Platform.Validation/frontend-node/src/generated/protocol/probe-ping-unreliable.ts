// <auto-generated />
// @ts-nocheck
// noinspection JSUnusedGlobalSymbols,JSUnusedLocalSymbols
import {
    TeleportObjectWriter,
    TeleportObjectReader,
    TeleportField,
    TeleportArrayReader,
    TeleportArrayElement,
    TeleportDictReader,
    TeleportDictEntry,
    TeleportGuid,
    TeleportType,
    ProtocolMessage,
    ProtocolMessageOverrides,
    createProtocolMessage,
    createProtocolMessageAsync,
    readProtocolMessagePayload,
    readProtocolMessagePayloadAsync
} from './ikon-teleport';

export interface ProbePingUnreliable
{
    Seq: bigint;
    SentAtMs: bigint;
    Origin: string;
    Mode: string;
    Note: string;
}

export const PROBE_PING_UNRELIABLE_VERSION = 1;
export const PROBE_PING_UNRELIABLE_OPCODE = 0x40000102;
export const PROBE_PING_UNRELIABLE_DEFAULT_FLAGS = 0x10; // MessageFlag.Unreliable

const PROBE_PING_UNRELIABLE_SEQ_FIELD_ID = 0x1995121B;
const PROBE_PING_UNRELIABLE_MODE_FIELD_ID = 0x2BA8ACD4;
const PROBE_PING_UNRELIABLE_ORIGIN_FIELD_ID = 0xDED36C02;
const PROBE_PING_UNRELIABLE_SENT_AT_MS_FIELD_ID = 0xE5C51DC5;
const PROBE_PING_UNRELIABLE_NOTE_FIELD_ID = 0xEBFED0E8;

export function createProbePingUnreliable(init?: Partial<ProbePingUnreliable>): ProbePingUnreliable
{
    const instance = {} as ProbePingUnreliable;
    resetProbePingUnreliable(instance);
    if (init)
    {
        Object.assign(instance, init);
    }
    return instance;
}

export function resetProbePingUnreliable(target: ProbePingUnreliable): ProbePingUnreliable
{
    target.Seq = 0n;
    target.SentAtMs = 0n;
    target.Origin = '';
    target.Mode = '';
    target.Note = '';
    return target;
}

export function encodeProbePingUnreliable(value: ProbePingUnreliable): Uint8Array
{
    const writer = new TeleportObjectWriter(PROBE_PING_UNRELIABLE_VERSION);
    writeToTeleportProbePingUnreliable(value, writer);
    return writer.finish();
}

export function writeToTeleportProbePingUnreliable(instance: ProbePingUnreliable, scope: TeleportObjectWriter): void
{
    scope.writeInt64Field(PROBE_PING_UNRELIABLE_SEQ_FIELD_ID, instance.Seq);
    scope.writeStringField(PROBE_PING_UNRELIABLE_MODE_FIELD_ID, instance.Mode);
    scope.writeStringField(PROBE_PING_UNRELIABLE_ORIGIN_FIELD_ID, instance.Origin);
    scope.writeInt64Field(PROBE_PING_UNRELIABLE_SENT_AT_MS_FIELD_ID, instance.SentAtMs);
    scope.writeStringField(PROBE_PING_UNRELIABLE_NOTE_FIELD_ID, instance.Note);
}

export function decodeProbePingUnreliable(data: ArrayBuffer | Uint8Array, destination?: ProbePingUnreliable): ProbePingUnreliable
{
    const reader = TeleportObjectReader.create(data);
    return readFromTeleportProbePingUnreliable(reader, destination);
}

export function readFromTeleportProbePingUnreliable(reader: TeleportObjectReader, destination?: ProbePingUnreliable): ProbePingUnreliable
{
    const instance = destination ?? createProbePingUnreliable();
    resetProbePingUnreliable(instance);
    readIntoProbePingUnreliable(reader, instance);
    return instance;
}

function readIntoProbePingUnreliable(reader: TeleportObjectReader, instance: ProbePingUnreliable): void
{
    let field: TeleportField | null;
    while ((field = reader.next()) !== null)
    {
        switch (field.fieldId)
        {
            case PROBE_PING_UNRELIABLE_SEQ_FIELD_ID:
            {
                if (field.isNull) throw new Error();
                instance.Seq = field.asInt64();
                break;
            }
            case PROBE_PING_UNRELIABLE_MODE_FIELD_ID:
            {
                if (field.isNull) throw new Error();
                instance.Mode = field.asString();
                break;
            }
            case PROBE_PING_UNRELIABLE_ORIGIN_FIELD_ID:
            {
                if (field.isNull) throw new Error();
                instance.Origin = field.asString();
                break;
            }
            case PROBE_PING_UNRELIABLE_SENT_AT_MS_FIELD_ID:
            {
                if (field.isNull) throw new Error();
                instance.SentAtMs = field.asInt64();
                break;
            }
            case PROBE_PING_UNRELIABLE_NOTE_FIELD_ID:
            {
                if (field.isNull) throw new Error();
                instance.Note = field.asString();
                break;
            }
            default:
            {
                break;
            }
        }
    }
}

export function toProtocolMessageProbePingUnreliable(value: ProbePingUnreliable, senderId: number, overrides?: ProtocolMessageOverrides): ProtocolMessage
{
    const payload = encodeProbePingUnreliable(value);
    return createProtocolMessage(PROBE_PING_UNRELIABLE_OPCODE, payload, PROBE_PING_UNRELIABLE_VERSION, senderId, { ...overrides, flags: (overrides?.flags ?? 0) | PROBE_PING_UNRELIABLE_DEFAULT_FLAGS });
}

export function fromProtocolMessageProbePingUnreliable(raw: ProtocolMessage | ArrayBuffer | Uint8Array, destination?: ProbePingUnreliable): ProbePingUnreliable
{
    const payload = readProtocolMessagePayload(raw, PROBE_PING_UNRELIABLE_OPCODE, PROBE_PING_UNRELIABLE_VERSION);
    return decodeProbePingUnreliable(payload, destination);
}

export async function toProtocolMessageProbePingUnreliableAsync(value: ProbePingUnreliable, senderId: number, overrides?: ProtocolMessageOverrides): Promise<ProtocolMessage>
{
    const payload = encodeProbePingUnreliable(value);
    return createProtocolMessageAsync(PROBE_PING_UNRELIABLE_OPCODE, payload, PROBE_PING_UNRELIABLE_VERSION, senderId, { ...overrides, flags: (overrides?.flags ?? 0) | PROBE_PING_UNRELIABLE_DEFAULT_FLAGS });
}

export async function fromProtocolMessageProbePingUnreliableAsync(raw: ProtocolMessage | ArrayBuffer | Uint8Array, destination?: ProbePingUnreliable): Promise<ProbePingUnreliable>
{
    const payload = await readProtocolMessagePayloadAsync(raw, PROBE_PING_UNRELIABLE_OPCODE, PROBE_PING_UNRELIABLE_VERSION);
    return decodeProbePingUnreliable(payload, destination);
}
