// <auto-generated />
/* eslint-disable */
// noinspection JSUnusedGlobalSymbols

/** The persisted-state guide's worked example of a schema-versioned type. It exists so the guide's UpgradeFrom1 migration is pinned to a partial half the Teleport compiler actually generates the twin for, rather than to a hand-written fence nothing compiles. */
export interface PlayerProfile
{
    DisplayName: string;
    Score: number;
}
